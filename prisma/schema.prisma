generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  schemas = ["uni-hungry"]
}

model Teste {
  id   Int    @id @default(autoincrement())
  name String
  
  @@schema("uni-hungry")
}